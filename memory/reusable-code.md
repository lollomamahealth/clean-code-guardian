# Reusable Code Index

This file indexes existing utilities in the project to prevent code duplication.
Updated automatically by the code-reuse-detector subagent.

## How to Use

Before creating new utility functions, search this index to find existing implementations.

---

## Utilities by Category

### Database Utilities

| Function | Location | Purpose |
|----------|----------|---------|
| *Index empty - run `/check-patterns` to populate* | | |

### LLM/AI Utilities

| Function | Location | Purpose |
|----------|----------|---------|
| `LLMClient` | `src/quantitative_agent/utils/` | Structured output via Instructor |
| `LLMConfig` | `src/quantitative_agent/utils/` | Configuration for LLM calls |

### Data Processing

| Function | Location | Purpose |
|----------|----------|---------|
| *Scan project to populate* | | |

### Validation/Parsing

| Function | Location | Purpose |
|----------|----------|---------|
| *Scan project to populate* | | |

### Test Fixtures

| Fixture | Location | Purpose |
|---------|----------|---------|
| *Check tests/fixtures/ to populate* | | |

---

## Recently Added

*New utilities will be logged here with timestamps*

---

## Search Tips

- Use grep patterns like `async def .*util` to find async utilities
- Check `__init__.py` files for exported functions
- Look in `utils/` directories first
